name: "CI: Nix Flake Validation"

on:
  push:
    branches: [ main ]
    paths:
      - '**.nix'
      - 'flake.lock'
      - 'scripts/millennium*.sh'
      - 'scripts/millennium-mcp.py'
      - 'scripts/common.sh'
      - 'scripts/lib/**'
      - 'completions/**'
      - 'man/**'
      - 'LICENSE'
      - '.github/workflows/nix.yml'
  pull_request:
    branches: [ main ]
    paths:
      - '**.nix'
      - 'flake.lock'
      - 'scripts/millennium*.sh'
      - 'scripts/millennium-mcp.py'
      - 'scripts/common.sh'
      - 'scripts/lib/**'
      - 'completions/**'
      - 'man/**'
      - 'LICENSE'
      - '.github/workflows/nix.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build Nix Package
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Nix
        uses: cachix/install-nix-action@v25
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Flake
        run: nix build --experimental-features "nix-command flakes" --no-link
