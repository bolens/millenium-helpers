name: "CI: PowerShell Script Analysis"

on:
  push:
    branches: [ main ]
    paths:
      - 'scripts/windows/**'
      - 'completions/powershell/**'
      - '.github/workflows/powershell-lint.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'scripts/windows/**'
      - 'completions/powershell/**'
      - '.github/workflows/powershell-lint.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write

jobs:
  psscriptanalyzer:
    name: PSScriptAnalyzer
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Run PSScriptAnalyzer (scripts/windows)
        uses: microsoft/psscriptanalyzer-action@v1.1
        with:
          path: scripts/windows
          recurse: true
          severity: '"Error"'
          enableExit: true
          excludeRule: '"PSAvoidUsingWriteHost","PSUseApprovedVerbs","PSAvoidUsingCmdletAliases","PSUseShouldProcessForStateChangingFunctions"'
          output: results-windows.sarif

      - name: Run PSScriptAnalyzer (completions/powershell)
        uses: microsoft/psscriptanalyzer-action@v1.1
        with:
          path: completions/powershell
          recurse: true
          severity: '"Error"'
          enableExit: true
          excludeRule: '"PSAvoidUsingWriteHost","PSUseApprovedVerbs","PSAvoidUsingCmdletAliases","PSUseShouldProcessForStateChangingFunctions"'
          output: results-completions.sarif

      - name: Upload SARIF (windows)
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results-windows.sarif
        continue-on-error: true

      - name: Upload SARIF (completions)
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results-completions.sarif
        continue-on-error: true
